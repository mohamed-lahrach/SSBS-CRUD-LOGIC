# Bus Trip Sandbox

A Django REST API for managing buses, routes, trips, and reservations.

## Setup

1. Clone the repository:
   ```bash
   git clone https://github.com/YOUR_USERNAME/bus_trip_sandbox.git
   cd bus_trip_sandbox
